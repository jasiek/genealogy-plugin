{
  "name": "genealogy",
  "description": "This plugin lets you query different data sources for information about your ancestors.",
  "version": "0.0.1",
  "author": {
    "name": "Jan Szumiec"
  },
  "userConfig": {
    "heredis_db": {
      "type": "file",
      "title": "Heredis database",
      "description": "Path to your local .heredis SQLite file. Leave empty to enable only the live research sources (Geneteka, Genealogia w Archiwach).",
      "required": false
    },
    "gedcom_path": {
      "type": "file",
      "title": "GEDCOM file",
      "description": "Path to a GEDCOM (.ged) file. Activates the gedcom_* tools as the verified-facts source. Use either this or heredis_db.",
      "required": false
    },
    "geneteka_min_interval": {
      "type": "string",
      "title": "Geneteka rate limit (seconds)",
      "description": "Minimum interval between Geneteka requests. Default 5.",
      "default": "5",
      "required": false
    },
    "genealogia_w_archiwach_min_interval": {
      "type": "string",
      "title": "Genealogia w Archiwach rate limit (seconds)",
      "description": "Minimum interval between Genealogia w Archiwach requests. Default 5.",
      "default": "5",
      "required": false
    },
    "genbaza_min_interval": {
      "type": "string",
      "title": "GenBaza rate limit (seconds)",
      "description": "Minimum interval between GenBaza requests. Default 5.",
      "default": "5",
      "required": false
    },
    "lubgens_min_interval": {
      "type": "string",
      "title": "Lubgens rate limit (seconds)",
      "description": "Minimum interval between Lubgens (regestry.lubgens.eu) requests. Default 5.",
      "default": "5",
      "required": false
    },
    "genpod_min_interval": {
      "type": "string",
      "title": "GenPod rate limit (seconds)",
      "description": "Minimum interval between GenPod requests. Default 5.",
      "default": "5",
      "required": false
    },
    "genpod_username": {
      "type": "string",
      "title": "GenPod username",
      "description": "Username for Projekt Podlasie GenPod (https://genpod.projektpodlasie.pl). Required to enable genpod_* tools.",
      "required": false
    },
    "genpod_password": {
      "type": "string",
      "title": "GenPod password",
      "description": "Password for Projekt Podlasie GenPod. Required to enable genpod_* tools.",
      "required": false,
      "sensitive": true
    }
  }
}
