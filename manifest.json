{
  "dxt_version": "0.1",
  "name": "polish-genealogy-mcp",
  "display_name": "Polish Genealogy",
  "version": "0.2.0",
  "description": "Heredis (read-only) + Geneteka and Genealogia w Archiwach search for Polish genealogy research.",
  "long_description": "Multi-source MCP server for Polish genealogy. Two tiers: (1) read-only access to a local Heredis SQLite file (verified facts), and (2) live search of the Geneteka parish-record indexes and Genealogia w Archiwach (research candidates). Live sources are rate-limited and use a browser-style User-Agent. Requires Python 3.10+ available on PATH; runtime dependencies are pip-installed into the extension directory on first run.",
  "author": {
    "name": "Jan Szumiec",
    "email": "jan.szumiec@gmail.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jszumiec/heredis-mcp"
  },
  "keywords": ["genealogy", "polish", "heredis", "geneteka", "mcp"],
  "license": "MIT",
  "server": {
    "type": "binary",
    "entry_point": "bin/run.sh",
    "mcp_config": {
      "command": "${__dirname}/bin/run.sh",
      "args": [],
      "env": {
        "HEREDIS_DB": "${user_config.heredis_db}",
        "GENETEKA_MIN_INTERVAL": "${user_config.geneteka_min_interval}",
        "GENEALOGIA_W_ARCHIWACH_MIN_INTERVAL": "${user_config.genealogia_w_archiwach_min_interval}",
        "GENPOD_USERNAME": "${user_config.genpod_username}",
        "GENPOD_PASSWORD": "${user_config.genpod_password}"
      }
    }
  },
  "user_config": {
    "heredis_db": {
      "type": "file",
      "title": "Heredis database",
      "description": "Path to your local .heredis SQLite file. Leave empty to enable only the live research sources (Geneteka, Genealogia w Archiwach).",
      "required": false
    },
    "geneteka_min_interval": {
      "type": "string",
      "title": "Geneteka rate limit (seconds)",
      "description": "Minimum interval between Geneteka requests. Default 5.",
      "default": "5",
      "required": false
    },
    "genealogia_w_archiwach_min_interval": {
      "type": "string",
      "title": "Genealogia w Archiwach rate limit (seconds)",
      "description": "Minimum interval between Genealogia w Archiwach requests. Default 5.",
      "default": "5",
      "required": false
    },
    "genpod_username": {
      "type": "string",
      "title": "GenPod username",
      "description": "Username for Projekt Podlasie GenPod (https://genpod.projektpodlasie.pl). Required to enable genpod_* tools.",
      "required": false,
      "sensitive": false
    },
    "genpod_password": {
      "type": "string",
      "title": "GenPod password",
      "description": "Password for Projekt Podlasie GenPod. Required to enable genpod_* tools.",
      "required": false,
      "sensitive": true
    }
  }
}
